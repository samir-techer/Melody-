const greeting = { init() { this.render(); this.bindEvents(); }, render() { const page = document.getElementById('page-greeting'); if(!page) return; const nickname = localStorage.getItem('melody_nickname') || 'Friend'; page.innerHTML = '<div class="greeting"><div class="greeting__content"><h1 class="greeting__greeting">Good Evening, ' + nickname + '.</h1><p class="greeting__welcome">Welcome to Melody.</p><div class="greeting__poem"><p>Every song tells a story.</p><p>Every melody holds a memory.</p><p>Every playlist becomes another chapter of your life.</p><br><p>Whether you are chasing dreams, celebrating victories,</p><p>finding peace, or healing from heartbreak...</p><p>Music has always been there.</p><br><p>This is more than a music player.</p><p>This is your personal soundtrack.</p><br><p>Take a deep breath.</p><p>Press play.</p><p>Enjoy every moment.</p></div><button class="btn btn--primary btn--large btn--full greeting__btn" id="start-listening-btn">Start Listening →</button></div></div>'; }, bindEvents() { const btn = document.getElementById('start-listening-btn'); if(!btn) return; btn.addEventListener('click', () => { localStorage.setItem('melody_hasSeenGreeting', 'true'); window.location.hash = '#home'; }); } }; define('ui/greeting.js', (module) => { module.exports = greeting; });